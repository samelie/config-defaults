/// <reference types="vitest/browser" />

// Browser-side setup file for worker-streams tests
// This runs in the browser environment before each test

import { afterEach, beforeEach } from "vitest";

declare global {
}

beforeEach(() => {
});

afterEach(() => {
});
